{
  "name": "scalatton",
  "version": "1.0.0",
  "description": "Scalatton TypeScript Tool",
  "scripts": {
    "compile": "rm -rv build/ && tsc",
    "test": "npm run compile && mocha build/test/*",
    "watch": "nodemon -w typescript/ --on-change-only -d 1 -e \"ts\" --exec \"npm run test\""
  },
  "author": "MULTA MEDIO Informationssysteme AG",
  "license": "UNLICENSED",
  "devDependencies": {
    "jquery": "^2.2.3",
    "nodemon": "^1.11.0",
    "tslint": "^5.7.0",
    "tslint-multamedio-rules": "git+ssh://mars:/var/lib/git/tslint-multamedio-rules#v2.1.0",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.42",
    "chai": "^4.1.2",
    "mocha": "^3.5.0",
    "tsc": "^1.20150623.0"
  }
}
